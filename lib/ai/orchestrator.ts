/**
 * AI agent orchestration — implemented in M5.
 */

export type InsightType = "conflict" | "reminder" | "schedule";

export async function runDailyDigest(): Promise<void> {
  // M5: batch insights per household
}